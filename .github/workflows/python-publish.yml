# This workflow uploads a Python package to PyPI when a release is created.
# Uses trusted publishing (OIDC) — no PyPI password stored in repo secrets.

name: Upload Python Package

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # for PyPI trusted publishing
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install build
      - name: Build distributions
        run: python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # A release cut for an already-published version is a no-op instead
          # of a hard failure (e.g. when a version was uploaded manually).
          skip-existing: true
